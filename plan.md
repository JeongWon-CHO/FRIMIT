# Frimit React Native MVP 기획·구현 계획

## 1. 제품 정의와 성공 기준

- 핵심 타깃: 스크린타임을 함께 줄이고 싶은 한국 대학생 친구 2~8명.
- 핵심 가치: 개인 한도가 아닌 친구들의 `공동 시간 풀`을 소비하며 서로 견제하고 동기부여한다.
- MVP 성공 기준:
  - 활성화된 친구 그룹 10개 이상으로 2주간 베타 운영.
  - 2주 차에 그룹원 2명 이상이 주 4일 이상 동기화한 그룹이 25% 이상.
- 친근하고 장난스러운 소셜 생산성 톤을 사용하되 순위, 꼴찌 표시, 공개적인 비난은 배제한다.
- 베타는 18세 이상 대상으로 한국어·한국 시간대 중심으로 운영한다.

## 2. MVP 핵심 기능과 규칙

### 공동 스크린타임

- 사용자는 최대 5개 활성 그룹에 참여하고, 그룹별 공동 일일 한도를 공유한다.
- 하루의 경계는 그룹 기준 시간대의 오전 6시이며 기본 시간대는 `Asia/Seoul`이다.
- 잔여시간은 `공동 한도 - 활성 멤버 누적 사용량`으로 계산한다.
- 한도를 다 쓰면 각자가 고른 앱을 잠근다. iOS 전용이며 Android는 초과시간만 누적한다.
  - 잠글지 말지는 기기가 판정한다. 서버는 잔여를 알려주고, 기기는 그것을 `내 누적 + 잔여`라는 차단선으로 바꿔 들고 있다가 백그라운드 임계값 콜백에서 스스로 잠근다. 네트워크가 끊겨도 잠긴다.
  - 잠긴 뒤에도 초과시간은 계속 누적한다. 다른 사람이 마저 쓰기 때문이다.
  - 다음 오전 6시에 잠금이 풀린다.
  - 차단선을 심는 것은 동기화가 돌아온 뒤이므로, 하루의 첫 몇 분과 앱을 오래 열지 않은 구간은 잠기지 않는다. 잠그지 못한 시간이 사라지는 것은 아니다 — 사용량은 그대로 누적된다.
- 75%, 90%, 100% 도달 시 그룹당 하루 한 번씩 자동 푸시와 활동 이벤트를 생성한다.
- 각 멤버는 그룹별로 추적할 앱·카테고리를 직접 선택한다.
  - 앱 이름과 사용 내역은 기기에만 저장한다.
  - 그룹에는 선택 개수, 합계 사용시간, 마지막 동기화 시각, 권한 상태만 공개한다.
- 선택 변경은 다음 오전 6시에 반영하며 변경 사실과 개수만 활동 내역에 남긴다.
- 여러 그룹의 선택 대상이 겹치면 같은 사용시간을 각 그룹에 독립적으로 반영한다.
- 계정당 활성 집계 기기는 휴대폰 1대로 제한하고 새 기기 등록 시 이전 기기를 비활성화한다.
- 백그라운드 동기화는 최선을 다해 수행하되 “실시간”을 보장하지 않고 마지막 동기화 시각을 항상 표시한다.

### 그룹 운영

- 초대 링크와 6자리 초대 코드로 가입한다.
- 관리자 포함 준비된 멤버가 2명 이상이면 관리자가 그룹을 즉시 시작할 수 있다.
- 시작 시점부터 첫 오전 6시까지도 공동 한도 전체를 제공한다.
- 시작 후 가입·탈퇴는 다음 오전 6시에 반영한다.
- 공동 한도·초기화 시각·시간대 변경은 활성 멤버 전원의 동의를 받은 뒤 다음 오전 6시에 적용한다.
- 변경안은 48시간 동안 유효하며 미동의자가 있으면 만료된다.
- 관리자는 탈퇴 전에 관리자 권한을 이전해야 한다. 활성 멤버가 2명 미만이면 그룹을 보관 상태로 전환한다.

### 공동 목표

- 그룹당 활성 목표는 1개만 허용한다.
- 기간은 7일·14일·30일 중 선택하며 다음 오전 6시에 시작한다.
- 모든 참여자에게 동일한 개인 목표량과 사용자 정의 단위를 부여한다.
- 그룹 진행률은 개인 달성률을 100%로 제한한 뒤 평균하여 계산한다.
- 목표 시작 시점의 멤버를 참여자로 고정하며 중간 가입자는 다음 목표부터 참여한다.
- 진행량은 숫자와 선택적인 짧은 메모로 직접 기록한다.
- 기록은 해당 Frimit 일자 안에서 수정·삭제할 수 있고 모든 변경은 활동 내역에 남긴다.
- 사진 인증과 친구 승인은 MVP에서 제외한다.

### 소셜 기능

- 활동 이벤트에 정해진 이모지 세트로 반응할 수 있다.
- `콕 찌르기`는 같은 발신자→수신자 기준 30분 쿨다운, 상대별 하루 10회로 제한한다.
- 사용자는 그룹별 반응·콕 찌르기 푸시를 음소거할 수 있다.
- 자유 채팅, 댓글, 공개 순위는 MVP에서 제외한다.

## 3. 사용자 흐름과 화면 구조

### 온보딩 (계정에 한 번)

1. Frimit 가치 소개
2. Apple 또는 Google 로그인
3. 닉네임과 프리셋 아바타 선택
4. 알림 사용 이유 안내 후 푸시 권한 요청
5. 스크린타임 수집 방식과 공개 범위 안내 후 OS 권한 요청

여기까지가 온보딩이고, 끝나면 오늘 탭으로 들어간다. 그룹이 없으면 오늘 탭이 빈 상태와 함께 그룹 흐름의 입구를 보여준다.

### 그룹 흐름 (그룹마다)

1. 그룹 만들기 또는 초대 링크·코드로 참여
2. 그룹 이름·색과 공동 한도 설정
3. 그룹별 추적 앱·카테고리 선택
4. 준비 완료 및 그룹 시작 대기

두 흐름을 나누는 이유는 성격이 다르기 때문이다. 앞은 계정에 한 번 하는 일이고, 뒤는 그룹을 만들거나 참여할 때마다 반복한다. 붙여 두면 온보딩이 끝난 줄 안 사람 앞에서 퍼널이 한 번 더 열린다. 진행 표시도 흐름마다 따로 두어 각 바가 가득 차는 순간이 그 흐름이 끝나는 순간이 되게 한다.

권한을 거부해도 앱 탐색은 허용하지만 공동 집계의 준비 멤버로는 인정하지 않는다. 권한이 해제되면 그룹에 앱 목록이 아닌 “동기화 불가” 상태만 공개한다.

### 하단 탭

- `오늘`: 모든 그룹의 잔여시간, 사용률, 초과 여부, 동기화 상태를 카드로 표시.
- `목표`: 참여 중인 공동 목표와 개인·그룹 진행률, 기록 입력.
- `활동`: 한도 단계 도달, 초과, 목표 기록, 반응, 멤버·규칙 변경 이벤트를 그룹 통합 피드로 표시.
- `MY`: 프로필, 활성 기기, 권한 상태, 그룹별 추적 설정, 알림·음소거, 로그아웃·계정 삭제.

### 주요 상세 화면

- 그룹 상세: 오늘의 공동 게이지, 멤버별 총 사용시간, 최근 동기화 시각, 초과분, 최근 7일 기록.
- 그룹 생성·관리: 이름, 프리셋 아이콘·색상, 공동 한도, 시간대, 초대, 시작, 관리자 이전.
- 규칙 변경: 변경안 비교, 멤버별 승인 상태, 적용 예정 시각.
- 목표 상세: 개인별 진행률, 그룹 평균, 기록 내역, 이모지 반응.
- 권한 복구: 플랫폼별 설정 이동과 복귀 후 재검사.
- 기록이 늦게 도착해 지난 일자의 합계가 바뀌면 기록은 보정하되 이미 종료된 날짜의 푸시는 다시 보내지 않는다.

## 4. 데이터 구조와 공개 계약

### 주요 모델

- `profiles`: 닉네임, 프리셋 아바타, 로케일
- `devices`: 플랫폼, 활성 여부, 푸시 토큰, 권한·마지막 동기화 상태
- `groups`: 이름, 관리자, 시간대, 상태, 초대 코드
- `group_memberships`: 역할, 준비 상태, 가입·탈퇴 적용 시각
- `group_rules`: 공동 한도, 오전 6시 초기화 규칙, 적용 버전
- `rule_proposals`, `rule_approvals`: 변경 내용, 승인·만료·적용 상태
- `tracking_status`: 그룹별 선택 개수, 현재·예약 설정 버전, 권한 상태
- `usage_snapshots`: 기기에서 전송한 기간별 누적 사용 초와 수집 시각
- `daily_member_usage`: 서버가 확정한 멤버별 일일 누적값
- `goals`, `goal_participants`, `goal_entries`: 목표, 고정 참여자, 수치 기록
- `activity_events`, `reactions`, `nudges`: 소셜 활동과 제한 상태

### 클라이언트–네이티브 계약

`UsageSnapshot`은 다음 값만 JavaScript와 서버에 전달한다.

- `groupId`
- `periodStart`
- `cumulativeSeconds`
- `collectedAt`
- `permissionState`
- `source`
- `sequence`

앱 이름, Android 패키지명, iOS 선택 토큰은 이 계약에 포함하지 않는다.

### 서버 동작

- 서버 시간을 기준으로 기간과 활성 멤버 여부를 검증한다.
- 스냅샷은 `device + group + period + sequence`로 멱등 처리한다.
- 누적값은 감소하지 않도록 서버에서 최대 관측값을 채택한다.
- Realtime으로 그룹 일일 합계, 목표 진행률, 활동 이벤트를 배포한다.
- 클라이언트가 집계값·규칙·시스템 이벤트를 직접 수정하지 못하게 RLS와 서버 RPC로 제한한다.
- 상세 스냅샷은 7일, 활동 내역은 14일, 일별 합계는 90일 후 삭제한다.
- 계정 삭제 시 프로필과 인증 정보를 제거하고, 진행 중인 기간의 공정성을 위해 필요한 합계만 익명 멤버 기록으로 보존 기간까지 유지한다.

## 5. 기술 스택과 구현 순서

### 앱

- Expo SDK 57, React Native 0.86, React 19.2, TypeScript strict mode, Expo Prebuild/CNG
- Expo Router 기반 파일 내비게이션
- Supabase JS, TanStack Query, Zustand, Zod
- React Native 기본 스타일과 공통 디자인 토큰, `react-native-svg` 기반 진행 게이지
- Expo Notifications와 EAS Development/Preview/Production Build
- Expo Go는 사용하지 않고 development build와 실제 기기를 기준으로 개발한다.

Expo SDK 57의 기본 지원 범위에 맞춰 iOS 16.4+, Android 7+를 최소 버전으로 둔다. 빌드에는 Xcode 26.4 이상이 필요하다. [Expo SDK 57](https://docs.expo.dev/versions/v57.0.0/)

### 네이티브 사용량 모듈

- iOS:
  - Family Controls, DeviceActivityMonitor extension, App Group을 Swift로 구현한다.
  - 시스템 `FamilyActivityPicker`로 앱·카테고리를 선택한다.
  - config plugin이 extension target, entitlement, App Group 설정을 clean prebuild마다 재생성한다.
  - 기기 내 합계만 App Group을 통해 호스트 앱에 전달한다.
- Android:
  - `UsageStatsManager`와 Usage Access 설정 화면을 Kotlin 모듈로 연결한다.
  - WorkManager로 허용 범위 내 주기적 집계를 시도하고 앱 시작·복귀 시 즉시 동기화한다.
  - 접근성 서비스와 다른 앱 차단 권한은 MVP에 포함하지 않는다.
- 두 플랫폼 모두 Expo Modules API를 사용한다. [Expo Modules API](https://docs.expo.dev/modules/overview/), [Expo App Extensions](https://docs.expo.dev/build-reference/app-extensions/)

#### iOS 사용량 정확도에 대한 정정

iOS에는 "지금까지 몇 초 썼는가"를 직접 묻는 API가 없다. Screen Time 데이터는 report extension 안에서만 읽히고 호스트 앱은 접근할 수 없으며, `DeviceActivityMonitor`는 미리 등록한 임계값을 넘었을 때만 콜백을 준다. 처음에는 두 경로를 합쳐 쓰려 했으나, 실기기 스파이크 결과 **임계값 경로 하나만 남겼다**.

- 백그라운드(채택): 임계값 이벤트를 그룹당 하나의 `DeviceActivityName`에 등록하고, 발화한 이벤트 이름에서 분을 되읽어 계단식으로 누적한다. 첫 10분은 1분, 이후 5·15·30분으로 성기게 깐다(그룹당 60개). 동시 감시는 시스템 한계상 20개이며 사용자당 5개 그룹이므로 여유가 있다.
- 앱 사용 중 정밀 보정(폐기, 2026-08-13): report extension을 숨김 뷰로 띄워 정밀 합계를 받아오려 했으나, 실기기 3차 측정까지 extension이 **한 번도 실행되지 않았다**. 뷰 크기·투명도, 뷰 컨트롤러 계층 구성까지 바꿔 봤지만 결과가 같았다. 심사 리스크도 함께 지고 있었으므로 타깃째 제거했다. 경위는 `docs/spike-protocol.md`의 측정 기록에 남아 있다.

따라서 iOS의 누적 사용량 해상도는 임계값 사다리 간격이 그대로 결정한다(초반 1분). 누적값이 줄어들지 않는다는 규칙은 기기·서버 양쪽에 동일하게 적용한다. Android는 `queryEvents`로 임의 구간을 직접 계산할 수 있어 이 제약이 없다.

### 백엔드와 운영

- Supabase Auth: Apple·Google 로그인
- Supabase Postgres, RLS, Realtime, Edge Functions, 예약 정리 작업
- Edge Function에서 Expo Push Service를 호출하고 한도 단계별 중복 푸시를 방지한다.
- 민감한 사용시간과 앱 정보는 분석 서비스로 전송하지 않는다.
- 제품 지표는 비식별 Supabase 이벤트로 집계하고, 오류 추적은 민감 필드를 제거한 Sentry를 사용한다.
- EAS Update는 JavaScript 변경에만 사용하고 네이티브 모듈 변경 시 runtime version과 새 바이너리를 배포한다.

### 구현 단계

1. iOS·Android 실제 기기에서 권한 요청, 앱 선택, 그룹별 누적 합계 추출, 로컬 저장을 검증하는 기술 스파이크
2. Expo Prebuild config plugin의 clean 재생성 및 EAS 빌드 검증
3. Supabase 스키마·RLS·집계 RPC와 인증 구현
4. 온보딩·그룹 생성·초대·활성화 구현
5. 오늘 화면, 동기화, Realtime 공동 풀, 임계 알림 구현
6. 공동 목표, 활동 피드, 반응·콕 찌르기 구현
7. 최근 7일 기록, 설정, 권한 복구, 계정 삭제 구현
8. TestFlight·Play 비공개 테스트 배포

iOS 백그라운드 자동 집계가 기술 스파이크에서 불안정하면 앱 열기·복귀 시 정확히 동기화하고, 백그라운드는 보조 수단으로 유지한다. UI의 마지막 동기화 시각과 권한 상태 계약은 그대로 유지한다.

## 6. 테스트와 베타 검증

- 오전 6시 경계, 시간대 유지, 여행·기기 시간 변경, 초과시간, 겹치는 그룹 집계를 단위 테스트한다.
- 75%·90%·100% 이벤트가 날짜별 한 번만 생성되는지 검증한다.
- 가입·탈퇴·선택 변경·전원 동의 규칙이 지정된 오전 6시에 반영되는지 검증한다.
- 목표 참여자 고정, 평균 진행률, 당일 수정·삭제 제한을 검증한다.
- RLS로 비멤버의 그룹·사용량·목표 접근과 다른 사용자의 기록 변경을 차단한다.
- 권한 거부·해제·복구, 오프라인 후 지연 업로드, 기기 교체, 앱 재설치 시나리오를 실제 기기에서 검증한다.
- 최대 8명 그룹, 사용자당 5개 그룹, 동시 Realtime 업데이트를 부하 테스트한다.
- clean prebuild를 반복해 iOS extension과 entitlement 생성이 멱등인지 CI에서 확인한다.
- 10~20개 친구 그룹에 클로즈드 베타를 배포하고 활성화율, 동기화 성공률, 권한 이탈률, 2주 차 그룹 유지율을 관찰한다.

## 가정과 MVP 제외 범위

- 무료 클로즈드 베타이며 결제·구독은 포함하지 않는다.
- Android의 앱 차단은 제외한다. `UsageStatsManager`에는 잠그는 수단이 없고, 접근성 서비스와 오버레이는 스토어 정책 위험이 크다. iOS만 잠기고 Android는 초과시간 표시에 머무른다.
- 차단 화면의 문구와 "1분만 더" 같은 우회 수단은 제외한다. iOS 기본 차단 화면을 그대로 쓴다.
- 집중 타이머, 사진 인증, 채팅, 댓글, 연락처 친구 찾기, 공개 순위, 웹·태블릿 지원은 제외한다.
- 사용량과 목표 기록은 사용자 기기와 직접 입력을 신뢰하며 기기 위변조 방지는 MVP 범위 밖이다.
