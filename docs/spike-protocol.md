# 기술 스파이크 검증 절차

plan.md 5장 구현 1단계에 해당한다. **이 문서의 목적은 앱을 만드는 게 아니라, 제품이 성립하는지 재는 것**이다.
여기서 나온 숫자가 공동 풀 UX의 해상도(잔여시간을 분 단위로 보여줄지, 5분 단위로 보여줄지)를 결정한다.

## 0. 준비

### Apple Team ID — ✅ 설정됨

extension 타깃은 호스트 앱과 같은 팀으로 서명돼야 하는데, Expo의 자동 서명은 앱 타깃만 챙긴다.
그래서 `app.json`의 plugin props에 팀 ID를 직접 넣어 두었다.

```json
["./plugins/withFrimitScreenTime", { "appleTeamId": "7D75P7PR8U" }]
```

다른 팀에서 빌드할 때는 `APPLE_TEAM_ID` 환경변수로 덮어쓸 수 있다.

Team ID를 찾을 때 흔히 틀리는 지점: 인증서 이름의 괄호 안 값이 아니라 **OU 필드**가 Team ID다.

```sh
security find-certificate -c "Apple Development" -p | openssl x509 -noout -subject
# subject=UID=..., CN=Apple Development: NAME (인증서 ID), OU=팀 ID, ...
```

### Family Controls 배포 엔타이틀먼트 — ✅ 완료 (2026-08-13)

Apple 승인을 받았고, 아래 세 App ID 모두에 Family Controls (Distribution) capability를 활성화했다.
TestFlight 배포를 막을 요소는 이 항목에서 더 없다.

| 역할 | Bundle ID |
|---|---|
| 호스트 앱 | `com.frimit.app` |
| Monitor extension | `com.frimit.app.activitymonitor` |
| ~~Report extension~~ (폐기) | ~~`com.frimit.app.activityreport`~~ |

두 값은 `plugins/withActivityExtensions.js`가 호스트 bundle ID로부터 자동 생성하는 이름과 일치한다.
호스트 bundle ID를 바꾸면 포털의 App ID도 함께 만들어야 한다.

Report extension은 3차 측정 뒤 폐기했다(아래 기록). 포털의 `com.frimit.app.activityreport` App ID는
이제 어느 빌드에도 쓰이지 않는다 — 지워도 되고, 나중을 위해 남겨 둬도 빌드에는 영향이 없다.

## 1. 실행

Expo Go는 쓰지 않는다. 네이티브 모듈이 있으므로 development build가 필요하다.

```sh
npx expo prebuild --clean          # 네이티브 프로젝트 재생성
npx expo run:ios --device          # 실기기 선택
npx expo run:android --device
```

시뮬레이터/에뮬레이터에서는 Family Controls도 UsageStats도 실제 데이터를 주지 않는다. **반드시 실기기**.

스파이크 화면은 더 이상 앱의 첫 화면이 아니다(온보딩과 오늘 화면이 그 자리에 들어왔다).
**MY 탭 → `스파이크 화면 열기`**로 들어가거나, 개발 중이라면 `/spike`로 직접 이동한다.
계측 화면은 제품 화면과 별개로 자기 그룹을 `ensureGroup`으로 확보하므로 온보딩을 건너뛰어도 동작한다.

## 2. iOS 검증 항목

앱의 스파이크 화면에서 1→2→3 순서대로 누르고 아래를 기록한다.

| # | 확인할 것 | 기록할 값 | 왜 중요한가 |
|---|---|---|---|
| I1 | 권한 요청 시트가 뜨는가 | 뜸/안 뜸, 오류 메시지 | entitlement가 제대로 붙었는지의 증거 |
| I2 | `FamilyActivityPicker`에 앱 목록이 나오는가 | 나옴/빈 목록 | 권한이 실제로 승인됐는지 |
| I3 | 선택 후 개수가 저장되는가 | 선택 개수 | 앱을 껐다 켜도 유지되는지 함께 확인 |
| I4 | 진단의 **App Group 연결** | 연결됨/안 됨 | ❗️안 되면 extension이 값을 못 넘긴다. 여기서 막히면 아래는 전부 무의미 |
| I5 | 집계 시작이 오류 없이 되는가 | 성공/실패 사유 | 동시 감시 20개 한계도 함께 확인 |
| I6 | **선택한 앱을 6분 이상 실제로 사용한 뒤** 누적값이 오르는가 | 첫 값이 뜨기까지 걸린 시간, 표시된 초 | 스파이크의 핵심. 이게 안 되면 제품이 성립하지 않는다 |
| I7 | 5분 임계값 간격이 실제로 지켜지는가 | 5/10/15분에서 각각 관측된 시각 | 계단 해상도의 실측치 |
| I8 | 앱을 완전히 종료한 뒤에도 누적이 이어지는가 | 종료 전/후 값 | 백그라운드 신뢰도. plan.md 175행의 판단 근거 |
| I9 | 기기 재부팅 후에도 감시가 살아있는가 | 유지/소실 | 소실되면 앱 실행 시 재등록 로직이 필요 |
| I10 | 오전 6시 경계에서 누적값이 0으로 돌아가는가 | 경계 전후 값 | 하루 경계 처리의 실측. 기기 시각을 조작하지 말고 실제로 넘겨서 확인 |
| ~~I11~~ | ~~**정밀 보정**을 눌렀을 때 값이 갱신되는가~~ | — | 전략 A와 함께 폐기 (3차 기록 참조) |
| ~~I12~~ | ~~정밀 보정에 걸리는 시간~~ | — | 위와 같음 |

I11·I12는 더 이상 측정하지 않는다. "정밀 보정" 버튼도 스파이크 화면에서 사라졌고,
`진단 정보`가 대신 계단값과 **extension이 마지막으로 기록한 시각**을 찍는다.
계단값이 멈췄을 때 "extension이 안 돈 것"과 "화면이 안 읽은 것"을 이 시각으로 구분한다.

### I6이 실패하면

임계값 이벤트가 발화하지 않는 흔한 원인들:

1. **저전력 모드** — DeviceActivity 콜백이 지연되거나 누락된다. 끄고 재시도.
2. **선택이 카테고리뿐** — 카테고리 단위 임계값은 개별 앱보다 발화가 늦다. 개별 앱을 직접 골라 재시도.
3. **이벤트가 너무 많음** — 현재 그룹당 60개를 등록한다. `FrimitScheduler.thresholdMinutes`를 5개 정도로 줄여 발화 여부만 먼저 확인.
4. **선택 저장이 extension에 안 보임** — I4로 돌아갈 것.

## 3. Android 검증 항목

| # | 확인할 것 | 기록할 값 |
|---|---|---|
| A1 | Usage Access 설정 화면이 열리는가 | 열림/안 열림 |
| A2 | 설정에서 켠 뒤 앱으로 돌아왔을 때 권한이 `granted`로 바뀌는가 | 바뀜/안 바뀜 |
| A3 | 설치 앱 목록이 나오는가 | 앱 개수 |
| A4 | 앱을 사용한 뒤 누적값이 오르는가 | 실제 사용 시간 vs 표시된 값의 차이 |
| A5 | 구간 시작 이전부터 켜져 있던 앱이 정확히 잘리는가 | 오차(초) |
| A6 | 앱 종료 후에도 값이 유지되는가 | 유지/소실 |

Android는 `queryEvents`로 직접 계산하므로 **앱이 실행되는 순간 항상 정확한 값**이 나와야 한다.
오차가 크다면 `FrimitUsage.foregroundSeconds`의 이벤트 짝짓기 로직을 의심할 것.
(1차 측정에서 실제로 여기가 틀렸다. 아래 기록 참조.)

A5는 오전 6시를 실제로 넘겨야 잴 수 있다. 앱을 켜 둔 채 경계를 넘긴 뒤 새 구간의 누적을 본다.

## 4. 판단 기준

스파이크의 결론은 셋 중 하나여야 한다.

- **통과** — I6, I8이 되고 계단 해상도가 5분 이내. plan.md 그대로 진행.
- **조건부 통과** — I6은 되지만 I8(백그라운드)이 불안정. plan.md의 대비책대로 "앱 열 때 정확히 동기화, 백그라운드는 보조"로 전환한다. (원래 여기서 전략 A를 앞당기려 했지만, 전략 A 자체가 폐기됐으므로 남은 수단은 임계값 사다리를 더 촘촘히 까는 것뿐이다.)
- **실패** — I4 또는 I6이 안 됨. 공동 풀의 근거가 되는 숫자를 못 얻으므로 제품 정의를 다시 봐야 한다.

## 5. 기록 남기기

측정값은 이 문서 아래에 날짜와 함께 덧붙인다. 나중에 iOS 업데이트로 동작이 바뀌었을 때
"예전에는 됐다"를 확인할 수 있는 유일한 근거가 된다.

---

## 측정 기록

### 2026-08-13 · iPhone 14 Pro · 1차

**결론: 조건부 통과.** 전략 B(백그라운드 임계값)는 동작하고, 전략 A(정밀 보정)는 동작하지 않았다.
공동 풀 제품의 근거가 되는 숫자는 확보됐으므로 plan.md대로 진행한다.

| 항목 | 결과 | 실측값 |
|---|---|---|
| I1 권한 시트 | ✅ | — |
| I2 앱 목록 | ✅ | 인스타그램·유튜브 2개 선택 |
| I3 선택 저장 | ✅ | 앱 재시작 후에도 유지 |
| I4 App Group | ✅ | `group.com.frimit.app` |
| I5 집계 시작 | ✅ | 구간 시작 `2026-08-12T21:00:00Z` = 08-13 06:00 KST |
| **I6 누적값 상승** | **✅** | **5분 임계값 발화, 계단 300초** |
| I11 정밀 보정 | ❌ | 정밀 0초 |

#### 확인된 것

- **오전 6시 경계 계산이 실기기에서 정확했다.** `periodStartFor`가 계산한 값이 그대로 찍혔다.
- **임계값 콜백은 실제로 온다.** 백그라운드에서 iOS가 콜백 → extension이 App Group에 기록 →
  호스트가 읽기까지의 사슬 전체가 동작한다. 이것이 제품 성립의 최소 조건이었다.

#### 알아낸 iOS 동작 두 가지

1. **`AuthorizationCenter.shared.authorizationStatus`는 앱 시작 직후 신뢰할 수 없다.**
   이미 승인된 상태인데도 몇 초간 `notDetermined`로 읽히다가 뒤늦게 `approved`로 바뀐다.
   한 번 읽고 끝내면 사용자에게 "권한 없음"으로 잘못 안내하게 되므로 **구독**해야 한다.
   → `onPermissionChange` 이벤트로 대응.

2. **보이지 않는 뷰에 붙인 `DeviceActivityReport`는 실행되지 않는 것으로 보인다.**
   1×1 크기에 `alpha = 0`으로 붙였더니 정밀값이 계속 0이었다.
   → 120×120 / `alpha = 0.02`로 바꾸고, 값이 아니라 **실행 시각**의 변화를 기다리도록 수정.

### 2026-08-13 · 2차 (임계값 1분 사다리 + 계측 추가)

| 항목 | 결과 |
|---|---|
| 임계값 사다리 60개 반영 | ✅ `감시 중 1개 / 임계값 60개` |
| 계단값 누적 | ✅ 300초 → **900초(15분)**, 반복적으로 안정 발화 |
| 정밀 보정 | ❌ `report extension이 한 번도 실행되지 않음` |

**전략 B는 확정적으로 동작한다.** 임계값 콜백이 일회성이 아니라 반복해서 온다는 것까지 확인됐다.

전략 A는 뷰 크기·투명도 문제가 아니었다. 계측 덕분에 "필터가 틀렸다"가 아니라
"아예 실행되지 않는다"로 원인이 좁혀졌다. 남은 가설은 **뷰 컨트롤러 계층 구성**이다 —
`UIHostingController`의 view만 window에 얹고 `addChild`/`didMove`를 하지 않으면
SwiftUI 생명주기가 완성되지 않아, 원격 뷰 연결이 시작되지 않는 것으로 보인다.
3차에서 이것까지 안 되면 전략 A는 폐기한다.

#### 다음 측정에서 확인할 것

`정밀 보정`을 누른 뒤 기록에 아래 중 무엇이 찍히는지로 원인이 갈린다.

- `⚠️ report extension이 한 번도 실행되지 않음` → 렌더링 자체가 안 됨. 뷰를 더 키우거나,
  숨김 프로브 대신 실제 화면에 `DeviceActivityReport`를 배치하는 방식으로 바꿔야 한다.
- `report 실행됨 HH:MM:SS · 원시값 0초` → 실행은 됐고 필터가 틀렸다. `DeviceActivityFilter`의
  세그먼트(`.hourly`)와 토큰 필터 구성을 다시 봐야 한다.

전략 A가 끝내 안 되더라도 제품은 성립한다. 그 경우 이 타깃을 제거하면 심사 리스크도 함께 사라진다.

### 2026-08-13 · 3차 (뷰 컨트롤러 계층 구성 후) — 전략 A 폐기

| 항목 | 결과 |
|---|---|
| 계단값 누적 | ✅ **900초(15분)**까지 누적, 1분 사다리로 반복 발화 |
| 정밀 보정 | ❌ `report extension이 한 번도 실행되지 않음` |

2차에서 남겨 둔 마지막 가설(`addChild`/`didMove`로 SwiftUI 생명주기를 완성한다)까지 적용했지만
`reportLastRunAt`은 끝내 `null`이었다. 세 번의 측정에서 바꿔 본 것은 다음과 같다.

| 차수 | 바꾼 것 | 결과 |
|---|---|---|
| 1차 | 1×1 / `alpha = 0` 숨김 뷰 | 실행 안 됨 |
| 2차 | 120×120 / `alpha = 0.02`, 실행 시각 계측 추가 | 실행 안 됨 (원인이 "필터"가 아니라 "미실행"임을 확인) |
| 3차 | `addChild` + `didMove`로 뷰 컨트롤러 계층 구성 | 실행 안 됨 |

**결론: 전략 A 폐기.** 2차에서 정한 기준("3차에서 이것까지 안 되면 폐기") 그대로다.
남은 선택지는 화면에 실제로 보이는 `DeviceActivityReport`를 배치하는 것뿐인데, 그건
"숫자만 조용히 얻는다"는 원래 목적과 다른 UI를 제품에 강제한다. 게다가 이 경로에는
심사 리스크가 붙어 있었으므로, 얻는 것보다 지고 가는 것이 크다.

#### 제거한 것 (2026-08-13)

- `FrimitActivityReport` 타깃 — config plugin의 `EXTENSIONS`에서 제외. 이미 만들어진
  프로젝트에서도 소스가 지워지도록 `RETIRED_EXTENSIONS`로 남겨 두었다.
- `plugins/extensions/report/`, `modules/screen-time/ios/FrimitReportProbe.swift`
- App Group의 report 관련 키 전부 (`frimit.usage.report.*`, `pendingReportGroup`, 계측 키 2개)
- JS API `refreshReport`, `getUsageBreakdown` → 임계값 경로만 보는 `getUsageDetail`로 대체

`npm run verify:prebuild`가 이제 이 타깃이 **되살아나지 않는 것까지** 확인한다.

### 2026-08-14 · 4차 (서버 연동 후) — 기기에서 서버까지 한 바퀴

동기화 계층을 붙이고 처음으로 **기기 → App Group → 호스트 → Supabase**를 끝까지 통과시켰다.

| 항목 | 결과 | 실측값 |
|---|---|---|
| I6 누적값 상승 | ✅ | 6분 0초 |
| **I8 백그라운드 신뢰도** | **✅** | 앱을 완전히 종료하고 5분 사용 후 복귀 — 계단값이 이어서 올랐다 |
| I9 재부팅 후 감시 유지 | ✅ | 유지됨 (단, 아래 참조) |
| 서버 확정값 | ✅ | `daily_member_usage` 360초 · `date_key 2026-08-14` · 순번 3 |
| 오전 6시 경계 | ✅ | `구간 시작 2026-08-14 06:00 KST`가 기기·서버 양쪽에서 같은 값 |
| 멱등성 | ✅ | 사용하지 않은 채 앱만 껐다 켜기 → `1건 중 기록 0 · 무시 1` |

**I8이 통과한 것이 이번 측정에서 가장 중요하다.** plan.md의 대비책("백그라운드가 불안정하면
앱 열 때 동기화하고 백그라운드는 보조로")을 쓰지 않아도 된다는 뜻이다.

서버에 원본 스냅샷이 한 건만 남은 것은 정상이다. 그룹을 시작하기 전에 올린 두 건은
`group_not_collecting`으로 거절됐고, 시작 후 첫 건이 들어왔다.

#### 여기서 드러난 결함 셋

1. **로컬 전용 group id가 계속 따라다녔다.** 예전 빌드의 `'spike-group-1'`이 기기에 남아
   매번 함께 업로드됐고 서버에서 uuid 캐스팅부터 실패했다. `진단 정보`의 `감시 중 2개`도
   같은 원인이다 — 죽은 그룹이 DeviceActivity 감시 슬롯(한도 20개)과 이벤트 60개를 계속
   잡고 있었다. 이제 업로드 전에 걸러 내고 추적까지 걷어낸다.
2. **거절 사유가 빈 문자열로 나갔다.** PostgreSQL은 hint 없는 오류에 NULL이 아니라 빈
   문자열을 준다. 화면에는 `거절 1()`처럼 괄호만 남았다. hint가 비면 SQLSTATE를 싣는다.
3. **같은 값을 다시 올리면 `recorded`로 보고됐다.** 기기의 sequence는 스냅샷을 읽을 때마다
   오르므로 앱을 껐다 켜기만 해도 멱등 키가 달라진다. 즉 재동기화의 가장 흔한 모양이 가장
   부정확하게 보고되고 있었다. 갱신 전 확정값과 비교해 `stale`로 판정하도록 고쳤다.

세 번째는 특히 **관측이 없었으면 못 봤을 종류**다. 확정값은 처음부터 올바르게(최대값으로만)
움직이고 있었고, 틀린 것은 "무엇을 했는지 말하는 문장"뿐이었다.

#### 이후 정확도를 올려야 한다면

정밀 합계라는 선택지가 사라졌으므로 해상도는 임계값 사다리가 전부다. 올릴 방법은 둘뿐이다.

1. `FrimitScheduler.thresholdMinutes`의 앞 구간을 더 촘촘히 (1분 → 30초 단위는 불가, 최소 단위가 분이다)
2. 이벤트 개수를 늘리기 — 현재 60개. 늘렸을 때 발화 신뢰도가 떨어지는지는 다시 측정해야 한다.

### 2026-08-14 · Android 실기기 1차 — 이벤트 짝짓기 결함 발견 후 통과

| 항목 | 결과 | 실측값 |
|---|---|---|
| A1 설정 화면 | ✅ | 열림 (아래 단서) |
| A2 복귀 후 권한 갱신 | ✅ | `notDetermined` → `granted` |
| A3 설치 앱 목록 | ✅ | 90개 |
| **A4 누적값 상승** | **✅ (수정 후)** | 5초 사용 → 1분 0초에서 1분 5초. 오차 없음 |
| A5 구간 시작 이전부터 켜져 있던 앱 | ⏸ 미측정 | 오전 6시를 실제로 넘겨야 잴 수 있다 → 2차에서 통과 |
| A6 앱 종료 후 유지 | ✅ | 완전 종료 후 5초 더 사용 → 1분 5초에서 1분 10초 |

**A1의 단서**: `ACTION_USAGE_ACCESS_SETTINGS`는 앱 목록 화면까지만 열어 준다. 사용자가 그
목록에서 Frimit을 직접 찾아 켜야 한다. iOS 권한 시트처럼 "이 앱을 허용하시겠습니까"가 아니다.
권한 복구 화면(plan.md 86행)의 안내 문구는 이 차이를 반영해야 한다.

**A6이 iOS만큼 중요하지 않은 이유**: Android는 스냅샷을 읽을 때마다 `queryEvents`로 구간
전체를 처음부터 다시 계산한다. 앱이 죽어 있는 동안 무엇도 놓치지 않으므로, iOS의 I8처럼
백그라운드 신뢰도가 제품 성립 조건이 되지 않는다. 대신 **계산이 틀리면 조용히 전부 틀린다** —
그게 아래에서 실제로 일어난 일이다.

#### 첫 측정에서 나온 값: 1분 사용 → 22시간 38분 41초

구간 시작 06:00에서 11시간 19분(40,760초) 지난 시점에 누적 81,521초가 찍혔다. **경과 시간의
정확히 2배**라는 것이 단서였다.

원인은 `FrimitUsage.foregroundSeconds`의 이 한 줄이었다.

```kotlin
val opened = openedAt.remove(packageName) ?: startMs
```

Android는 앱 하나를 닫을 때 `ACTIVITY_PAUSED`와 `ACTIVITY_STOPPED`를 **둘 다** 보낸다.
첫 번째에서 짝이 맞아 정상적으로 더해지고, 뒤따르는 두 번째는 짝이 없어 `?: startMs`로
떨어져 **구간 전체를 한 번 더** 더한다. "구간 시작 이전부터 켜져 있던 앱"을 위한 되돌림이
모든 짝 없는 닫힘에 적용된 것이다. 여닫은 횟수만큼 구간 전체가 반복해서 쌓인다.

고친 것 셋:

1. `startMs` 되돌림을 **이 구간에서 그 앱의 이벤트를 처음 볼 때만** 적용한다. 그 뒤의 짝 없는
   닫힘은 중복이므로 버린다.
2. 이미 열려 있는 앱에 열림이 또 오면 시작 시각을 덮어쓰지 않는다. 덮어쓰면 그 사이가 사라진다.
3. 다른 앱이 올라오거나 화면이 꺼지면 열려 있던 구간을 그 시점에 끊는다. 포그라운드는 하나뿐이다.
   닫힘 이벤트를 하나 놓쳐도 구간이 endMs까지 무한정 자라지 않는다.

3번이 없으면 이벤트 하나를 놓칠 때마다 몇 시간짜리 유령 사용량이 생긴다. 1번만 고치는 것으로는
같은 종류의 다음 사고를 막지 못한다.

#### 여기서 드러난 서버 쪽 결함

**서버가 물리적으로 불가능한 값을 그대로 받아 확정했다.** 0005의 검증은
`cumulative_seconds between 0 and 90000`뿐이었는데, 그 상한은 서머타임으로 25시간이 되는
날을 위한 여유값이지 사용량의 상한이 아니다. plan.md 121행이 "서버 시간을 기준으로 기간을
검증한다"고 정해 두었지만 구간의 **시작**만 보고 길이는 보지 않고 있었다.

확정값은 최대값으로만 움직이므로(plan.md 123행) 이런 값은 한 번 들어오면 그날 하루를 통째로
오염시킨다. 기기를 고쳐도 서버는 계속 22시간을 들고 있어서, 이날 확정값 행을 손으로 지워야
재측정을 이어갈 수 있었다. **되돌릴 수 없는 값일수록 들어올 때 막아야 한다.**

→ `20260814000006_usage_period_bound.sql`. 상한은 `least(now(), 구간 끝) - 구간 시작`이고
여유는 15분(기기 시계 오차용)이다. 거절돼도 잃는 것은 없다 — 누적값은 단조 증가하고 앱은
복귀할 때마다 다시 올리므로 다음 동기화에서 통과한다.

이 사고는 원본 스냅샷 층이 왜 필요한지도 함께 보여줬다. `usage_snapshots`에 81521이 세 번,
그다음 고친 빌드의 51 → 61 → 65 → 70초가 순서대로 남아 있어서, 무엇이 언제 바뀌었는지를
확정값 하나만 봤을 때와 달리 그대로 읽을 수 있었다.

#### 이틀 뒤에 드러난 것: 구간이 넘어가지 않는다 (Android만)

08-16에 앱을 열었더니 확정값이 **`date_key 2026-08-14`**로 들어왔다. 이틀 내내 같은 구간으로
올리고 있었다는 뜻이다.

기기에 저장된 구간 시작은 "집계 시작"을 누른 순간의 값인데, 그것을 오전 6시에 옮겨 줄 주체가
Android에는 없다. iOS는 `DeviceActivitySchedule`이 매일 반복되므로 extension의
`intervalDidStart`가 `resetInterval`로 새 구간을 적어 준다(`FrimitActivityMonitor.swift:23`).
Android에는 그런 콜백이 아예 없고, `FrimitStore`에 한 번 저장된 값이 그대로 남는다.

결과는 조용하다. 오류도 거절도 없이 어제와 오늘의 사용량이 한 칸에 합쳐지고, 날짜 라벨만
이틀 전을 가리킨다. **0006의 한계값 가드도 이건 못 잡는다** — 지난 구간의 상한은 구간 길이
전체(24시간)라 그냥 통과한다. 가드는 "안 흐른 시간을 썼다"를 잡지 "엉뚱한 날에 넣었다"는
잡지 못한다.

→ `syncUsage`가 **올린 뒤에** 낡은 구간을 새 경계로 다시 무장한다(`rollTrackingPeriods`).
순서가 중요하다. 먼저 넘기면 지난 구간의 마지막 몇 분이 사라진다 — Android는 읽을 때마다
새 구간 기준으로 다시 계산하므로 되찾을 방법이 없다.

낡았는지는 **시각이 아니라 날짜 라벨로** 견준다(`isStalePeriod`). iOS가 적는 구간 시작은
콜백이 실제로 불린 시각이라 정확히 06:00:00이 아니어서, 시각을 그대로 견주면 매번 다르다고
판단해 다시 무장하고 `setPeriodStart`가 그날 누적을 0으로 되돌린다. 하루치가 조용히
사라지는 경로라 회귀 테스트를 붙여 두었다.

#### 다음 측정에서 확인할 것

- **A5** — 선택한 앱을 켜 둔 채로 오전 6시를 넘긴 뒤, 새 구간의 누적이 06:00부터만 세는지.
  이것만이 `startMs` 되돌림 경로가 실제로 도는 유일한 측정이다. 구간 롤오버가 함께 확인된다.
- 기기 재부팅 후에도 값이 유지되는지 (Android는 매번 재계산이므로 유지돼야 정상).

기기 시각을 임의로 옮겨 재는 방법은 쓰지 않는다. 서버가 기기 시각을 믿지 않으므로
(plan.md 121행) 미래로 옮긴 구간은 `future_period`로 거절되고, 뒤로 되돌리면 `UsageStats`의
일별 버킷이 어긋난다. iOS I10에 같은 주의가 이미 적혀 있다.

### 2026-08-17 · Android 실기기 2차 — A5 통과, 그리고 올리는 순서의 결함

인스타그램을 05:20경부터 10:05까지 포그라운드에 둔 채(충전 중, 화면 켜짐 유지) 오전 6시를
넘겼다. Frimit은 백그라운드에서 완전히 종료한 상태였다.

| 항목 | 결과 | 실측값 |
|---|---|---|
| **A5 구간 시작 이전부터 켜져 있던 앱** | **✅** | 구간 시작 06:00:00, 누적 4시간 5분 29초 → 닫힌 시각 10:05:29. 06:00 이전 40분은 잘려 나갔다 |
| 구간 롤오버 (앱이 닫힌 채 경계 통과) | ✅ | 구간 시작이 08-16 06:00 → **08-17 06:00**, 일자 라벨 2026-08-17 |
| 권한·선택 유지 | ✅ | 장시간 방치 후에도 `granted`, 선택 변경 시각 그대로 |

되돌림이 틀렸다면 05:20부터 세어 4시간 45분대가 나왔을 것이다. 초 단위로 맞았다.

#### 기기는 맞았는데 서버는 틀렸다

같은 시각 서버에는 이렇게 들어가 있었다.

```
date_key 2026-08-16 · period_start 08-16 06:00 KST · cumulative 16,936초 (seq 48)
```

**08-17 06:00 이후의 4시간이 08-16 칸에 들어갔다.** 08-14 행에서 한 번 봤던 오염이 일회성
잔재가 아니라 경계를 넘길 때마다 반복되는 구조였다.

원인은 `rollTrackingPeriods`의 "올린 뒤에 넘긴다"는 순서 자체에 있었다. 그 순서는 지난 구간의
마지막 몇 분을 지키려고 고른 것인데, **올릴 때 네이티브가 계산하는 창이 `[옛 구간 시작, 지금]`
이라 경계 너머까지 뻗는다.** 지키려던 마지막 몇 분과 함께 오늘 아침 네 시간이 딸려 들어왔다.

0006의 가드도 못 잡는다. 옛 구간의 상한은 구간 길이 전체(24시간)이고 16,936초는 그 안이다.
가드는 "안 흐른 시간을 썼다"를 잡지 "구간이 끝난 뒤의 시간을 썼다"는 잡지 못한다.

고친 것 둘:

1. **네이티브가 구간 끝을 안다.** `startTracking`이 `periodEnd`를 함께 받아
   `FrimitStore`에 보관하고, 스냅샷은 `min(지금, 구간 끝)`까지만 센다. 이제 순서와 무관하게
   옛 구간의 값은 경계에서 잘린다. 끝 계산은 `frimit-day.ts` 한 곳에만 두었다 — 서머타임이
   있는 시간대에서 하루는 24시간이 아니고, 그 규칙을 네이티브 두 곳에 다시 쓸 이유가 없다.
   iOS는 이 인자를 받고 버린다. `intervalDidStart`가 경계에서 구간을 스스로 넘기기 때문이다.
2. **넘긴 직후 새 구간의 첫 값을 바로 올린다.** 이 측정에서 서버에는 08-17 행이 아예 없었다.
   경계를 넘긴 뒤 앱을 닫아 버리면 다른 구성원 화면에서 그 사람은 오늘 하나도 안 쓴 것으로
   보인다. 공동 풀이 제품의 전부이므로 이건 표시 문제가 아니라 규칙이 어긋나는 문제다.

**남는 한계**: 앱이 이틀 이상 닫혀 있으면 중간 날은 영영 비어 있다. 롤오버는 저장된 구간에서
현재 구간으로 한 번에 건너뛰고, 건너뛴 날의 값을 올릴 경로가 없다. `UsageStats`에는 이벤트가
남아 있으므로 되찾는 것 자체는 가능하지만, 임의 구간을 조회하는 네이티브 API를 새로 열어야
한다. 지금은 열지 않는다.
