/**
 * 개발 중 바로 열 화면.
 *
 * 시뮬레이터에서는 딥링크가 iOS의 "이 앱에서 열까요?" 확인창에 막히고, 탭을
 * 대신 눌러 줄 방법이 없다. 이 상수를 한 줄 고치면 fast refresh가 그 화면으로
 * 옮겨 준다 — 8개 상태와 16개 온보딩 화면을 눈으로 도는 동안 이게 유일하게
 * 빠른 길이다.
 *
 * `null`이면 평소대로 `resolveEntryRoute()`의 판정을 따른다.
 * **릴리스 전에 반드시 null로 되돌린다.** `__DEV__` 밖에서는 무시된다.
 */
export const DEV_ROUTE: string | null = null;
